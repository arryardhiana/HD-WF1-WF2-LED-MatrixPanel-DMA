---
phase: 01-mqtt-json-refactor
plan: 02
type: execute
wave: 1
depends_on: ["01-01"]
files_modified:
  - platformio.ini
  - src/HD-WF1-WF2-LED-MatrixPanel-DMA.ino.cpp
autonomous: true
---

<objective>
## Goal
Implementasi JSON parser untuk topic `parking/{gate_id}/event` dan `parking/{gate_id}/greeting` menggunakan ArduinoJson v7. Handler placeholder dari plan 01-01 diganti dengan parser yang mengisi GateState dengan data nyata.

## Purpose
Plan 01-01 membuat routing dan handler placeholder. Plan ini mengisi handler tersebut dengan JSON parsing nyata sehingga GateState terisi dengan data benar dari sistem ANPR — status, plate, anprStatus, gateName, greeting.

## Output
- `platformio.ini`: ArduinoJson v7 ditambahkan ke lib_deps
- `src/HD-WF1-WF2-LED-MatrixPanel-DMA.ino.cpp`:
  - `#include <ArduinoJson.h>` ditambahkan
  - `mqttClient.setBufferSize(512)` dipanggil di `setup()`
  - `handleEventTopic()` mengparse JSON event payload ke GateState
  - `handleGreetingTopic()` mengparse JSON greeting payload ke GateState.greeting
</objective>

<context>
## Project Context
@.paul/PROJECT.md

## Prior Work
@.paul/phases/01-mqtt-json-refactor/01-01-SUMMARY.md

## Source Files
@src/HD-WF1-WF2-LED-MatrixPanel-DMA.ino.cpp
@platformio.ini
</context>

<acceptance_criteria>

## AC-1: ArduinoJson tersedia dan buffer MQTT cukup
```gherkin
Given platformio.ini diupdate dan firmware dikompilasi
When build dijalankan
Then ArduinoJson v7 ter-include tanpa error
  And mqttClient.setBufferSize(512) dipanggil sebelum setCallback di setup()
```

## AC-2: handleEventTopic() mem-parse JSON dengan benar
```gherkin
Given firmware berjalan dan MQTT terhubung
When payload JSON dikirim ke topic "parking/gate-a/event":
  {"status":"granted","plate":"D 1234 ABC","anpr_status":"detected","gate_name":"Gate A"}
Then gGate.status = "granted"
  And gGate.plate = "D 1234 ABC"
  And gGate.anprStatus = "detected"
  And gGate.gateName = "Gate A"
  And redrawNeeded = true
  And Serial log menampilkan nilai yang diparsed
```

## AC-3: handleEventTopic() robust terhadap payload tidak valid
```gherkin
Given firmware berjalan
When payload tidak valid dikirim (bukan JSON, atau field hilang)
Then firmware tidak crash
  And gGate.status = "idle" (field hilang = fallback ke default)
  And Serial log menampilkan pesan error parsing
```

## AC-4: handleGreetingTopic() mem-parse JSON greeting
```gherkin
Given firmware berjalan
When payload JSON dikirim ke topic "parking/gate-a/greeting":
  {"text":"Selamat Datang, Pak Budi!","scroll":false,"color":"white"}
Then gGate.greeting = "Selamat Datang, Pak Budi!"
  And redrawNeeded = true
  And Serial log menampilkan teks greeting yang diparsed
```

## AC-5: handleGreetingTopic() fallback jika field "text" kosong
```gherkin
Given firmware berjalan
When payload JSON greeting tidak memiliki field "text" atau nilainya kosong
Then gGate.greeting tidak berubah dari nilai sebelumnya
```

</acceptance_criteria>

<tasks>

<task type="auto">
  <name>Task 1: Tambah ArduinoJson ke platformio.ini dan setBufferSize di setup()</name>
  <files>platformio.ini, src/HD-WF1-WF2-LED-MatrixPanel-DMA.ino.cpp</files>
  <action>
    1. Di `platformio.ini`, tambahkan ArduinoJson ke `lib_deps_external` di section `[common]`:
       ```ini
       bblanchon/ArduinoJson@^7.0
       ```
       Letakkan setelah baris `knolleary/PubSubClient@^2.8`.

    2. Di `src/HD-WF1-WF2-LED-MatrixPanel-DMA.ino.cpp`, tambahkan include di bagian atas
       setelah `#include <PubSubClient.h>`:
       ```cpp
       #include <ArduinoJson.h>
       ```

    3. Di fungsi `setup()`, tambahkan `mqttClient.setBufferSize(512)` SEBELUM baris
       `mqttClient.setServer(MQTT_HOST, MQTT_PORT)`:
       ```cpp
       mqttClient.setBufferSize(512);
       ```
       Ini memastikan JSON payload event (~150 bytes) dapat diterima penuh.

    Jangan ubah hal lain di setup() atau platformio.ini.
  </action>
  <verify>
    Grep untuk "ArduinoJson" di platformio.ini dan source file masing-masing ditemukan 1 kali.
    Grep untuk "setBufferSize" di source file ditemukan 1 kali di setup().
  </verify>
  <done>AC-1 satisfied: ArduinoJson tersedia dan buffer MQTT diperbesar ke 512 bytes</done>
</task>

<task type="auto">
  <name>Task 2: Implementasi JSON parser di handleEventTopic()</name>
  <files>src/HD-WF1-WF2-LED-MatrixPanel-DMA.ino.cpp</files>
  <action>
    Ganti isi `handleEventTopic()` yang saat ini berisi placeholder dengan parser JSON nyata:

    ```cpp
    void handleEventTopic(const String& payload) {
      JsonDocument doc;
      const DeserializationError err = deserializeJson(doc, payload);

      if (err) {
        Serial.printf("[event] JSON parse error: %s | payload: %s\n",
                      err.c_str(), payload.c_str());
        gGate.status = "idle";
        redrawNeeded = true;
        return;
      }

      // Ekstrak fields — gunakan default jika field tidak ada
      const char* status = doc["status"] | "idle";
      const char* plate  = doc["plate"]  | "";
      const char* anpr   = doc["anpr_status"] | "";
      const char* gate   = doc["gate_name"]   | gGate.gateName.c_str();

      // Validasi nilai status
      const String s(status);
      if (s == "granted" || s == "denied" || s == "scanning" || s == "idle") {
        gGate.status = s;
      } else {
        gGate.status = "idle";
        Serial.printf("[event] unknown status value: %s\n", status);
      }

      gGate.plate      = String(plate);
      gGate.anprStatus = String(anpr);
      if (strlen(gate) > 0) gGate.gateName = String(gate);

      redrawNeeded = true;
      Serial.printf("[event] status=%s plate=%s anpr=%s gate=%s\n",
                    gGate.status.c_str(), gGate.plate.c_str(),
                    gGate.anprStatus.c_str(), gGate.gateName.c_str());
    }
    ```

    Catatan implementasi:
    - Gunakan `JsonDocument` (bukan `StaticJsonDocument` — deprecated di ArduinoJson v7)
    - Operator `| "default"` adalah idiom ArduinoJson v7 untuk nilai default jika field null/tidak ada
    - Validasi enum status agar tidak ada nilai tak terduga masuk ke GateState
    - JANGAN mengubah handler lain (handleGreetingTopic, handleConfigTopic, handleResetTopic)
  </action>
  <verify>
    Grep untuk "deserializeJson" di source — ditemukan di handleEventTopic.
    Grep untuk "JsonDocument" — ditemukan 1 kali (atau lebih jika Task 3 sudah ada).
    Grep untuk "placeholder" — tidak ditemukan di handleEventTopic.
  </verify>
  <done>AC-2 dan AC-3 satisfied: JSON event diparsed ke GateState dengan validasi dan fallback</done>
</task>

<task type="auto">
  <name>Task 3: Implementasi JSON parser di handleGreetingTopic()</name>
  <files>src/HD-WF1-WF2-LED-MatrixPanel-DMA.ino.cpp</files>
  <action>
    Ganti isi `handleGreetingTopic()` yang saat ini berisi placeholder dengan parser JSON:

    ```cpp
    void handleGreetingTopic(const String& payload) {
      JsonDocument doc;
      const DeserializationError err = deserializeJson(doc, payload);

      if (err) {
        Serial.printf("[greeting] JSON parse error: %s | payload: %s\n",
                      err.c_str(), payload.c_str());
        return;
      }

      // Hanya update jika field "text" ada dan tidak kosong
      const char* text = doc["text"] | "";
      if (strlen(text) > 0) {
        gGate.greeting = String(text);
        redrawNeeded   = true;
        Serial.printf("[greeting] text=%s\n", gGate.greeting.c_str());
      } else {
        Serial.println("[greeting] 'text' field missing or empty — greeting unchanged");
      }
      // Field "scroll" dan "color" disimpan untuk Phase 3 rendering
    }
    ```

    Catatan:
    - Field `scroll` dan `color` sengaja tidak diimplementasi di sini — Phase 3 yang akan menangani
    - Jika "text" kosong atau tidak ada, `gGate.greeting` tidak berubah (AC-5)
    - JANGAN mengubah `handleConfigTopic` atau `handleResetTopic`
  </action>
  <verify>
    Grep untuk "deserializeJson" di source — ditemukan 2 kali (handleEventTopic + handleGreetingTopic).
    Grep untuk "greeting unchanged" — ditemukan 1 kali di handleGreetingTopic.
  </verify>
  <done>AC-4 dan AC-5 satisfied: JSON greeting diparsed dengan fallback jika text kosong</done>
</task>

</tasks>

<boundaries>

## DO NOT CHANGE
- `MqttDisplayVirtualPanel` class dan `getCoords()` override
- `kDisplayConfigs[]` dan `setupDisplayForConfig()`
- `connectWiFiIfNeeded()` dan `connectMqttIfNeeded()`
- `handleConfigTopic()` dan `handleResetTopic()` — disentuh di plan 01-03
- `mqttCallback()` routing logic — sudah benar dari plan 01-01
- `renderPanels()` — disentuh di Phase 3
- `GateState` struct definition — tidak perlu berubah
- `topicEvent/Greeting/Config/Reset()` helper functions

## SCOPE LIMITS
- Plan ini TIDAK mengimplementasi `scroll` atau `color` dari greeting (Phase 3)
- Plan ini TIDAK mengimplementasi NVS persistence (plan 01-03)
- Plan ini TIDAK mengimplementasi display timeout/idle timer (Phase 4)
- Hanya 2 handler yang diupdate: `handleEventTopic` dan `handleGreetingTopic`

</boundaries>

<verification>
Sebelum plan dinyatakan selesai:
- [ ] `bblanchon/ArduinoJson@^7.0` ada di platformio.ini
- [ ] `#include <ArduinoJson.h>` ada di source file
- [ ] `mqttClient.setBufferSize(512)` ada di setup() sebelum setServer()
- [ ] `deserializeJson` ditemukan 2 kali (event + greeting handler)
- [ ] `JsonDocument` digunakan (bukan StaticJsonDocument)
- [ ] `handleEventTopic` memvalidasi nilai status enum
- [ ] `handleGreetingTopic` tidak mengubah greeting jika text kosong
- [ ] Grep: tidak ada referensi ke `placeholder` di kedua handler
</verification>

<success_criteria>
- ArduinoJson v7 terintegrasi ke project
- handleEventTopic() mengisi GateState.status/plate/anprStatus/gateName dari JSON
- handleGreetingTopic() mengisi GateState.greeting dari JSON dengan fallback
- Kedua handler robust terhadap JSON tidak valid (tidak crash)
- Build kompilasi sukses (jika environment mendukung)
</success_criteria>

<output>
Setelah selesai, buat `.paul/phases/01-mqtt-json-refactor/01-02-SUMMARY.md`
</output>
