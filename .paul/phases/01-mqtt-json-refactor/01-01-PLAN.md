---
phase: 01-mqtt-json-refactor
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/HD-WF1-WF2-LED-MatrixPanel-DMA.ino.cpp
autonomous: true
---

<objective>
## Goal
Ganti struct `SensorBinding` + `sensorValues[4]` dengan `GateState` dan `GateConfig`, lalu ganti 4 topic MQTT lama (am-102/sensor/*) dengan 4 topic baru schema parking gate. MQTT callback dirouting berdasarkan topic ke handler terpisah.

## Purpose
Baseline kode adalah sensor display generik. Plan ini adalah refactor fondasi: membentuk data model baru untuk parking gate agar plan berikutnya (JSON parser, rendering) bisa dibangun di atas struktur yang benar.

## Output
- `src/HD-WF1-WF2-LED-MatrixPanel-DMA.ino.cpp` direfactor:
  - `SensorBinding` dihapus, diganti `GateState` struct
  - `sensorValues[4]` dihapus
  - Topic MQTT diganti ke schema `parking/{gate_id}/*`
  - `mqttCallback` dirouting ke 4 handler function
  - `connectMqttIfNeeded` subscribe ke 4 topic baru
  - `renderPanels()` menggunakan `GateState` (sementara tampilkan raw string, rendering proper di Phase 3)
- Build kompilasi sukses tanpa error
</objective>

<context>
## Project Context
@.paul/PROJECT.md
@.paul/ROADMAP.md

## Source Files
@src/HD-WF1-WF2-LED-MatrixPanel-DMA.ino.cpp
</context>

<acceptance_criteria>

## AC-1: GateState menggantikan sensorValues
```gherkin
Given firmware dikompilasi setelah refactor
When build dijalankan (pio run)
Then tidak ada referensi ke `kSensors`, `sensorValues`, atau `SensorBinding` di source
  And `GateState` struct ada dengan field: status, plate, anprStatus, gateName, greetingText
```

## AC-2: MQTT topics baru ter-subscribe
```gherkin
Given firmware berjalan dan terhubung ke MQTT broker
When MQTT connect berhasil
Then Serial log menampilkan subscribe ke 4 topic:
  - "parking/gate-a/event"
  - "parking/gate-a/greeting"
  - "parking/gate-a/config"
  - "parking/gate-a/reset"
  And TIDAK subscribe ke topic am-102/* lama
```

## AC-3: Callback routing ke handler terpisah
```gherkin
Given MQTT callback dipanggil dengan topic tertentu
When topic adalah "parking/gate-a/event"
Then fungsi handleEventTopic() dipanggil
When topic adalah "parking/gate-a/greeting"
Then fungsi handleGreetingTopic() dipanggil
When topic adalah "parking/gate-a/config"
Then fungsi handleConfigTopic() dipanggil
When topic adalah "parking/gate-a/reset"
Then fungsi handleResetTopic() dipanggil
```

## AC-4: Display masih berfungsi setelah refactor
```gherkin
Given firmware berjalan
When tidak ada MQTT event masuk
Then display menampilkan data dari GateState default (idle state)
  And tidak ada crash atau infinite loop
```

</acceptance_criteria>

<tasks>

<task type="auto">
  <name>Task 1: Definisi GateConfig, GateState, dan topic strings</name>
  <files>src/HD-WF1-WF2-LED-MatrixPanel-DMA.ino.cpp</files>
  <action>
    1. Hapus struct `SensorBinding` dan array `kSensors[4]`.
    2. Hapus array `sensorValues[4]`.
    3. Tambahkan konstanta konfigurasi gate di bawah MQTT credentials:
       ```cpp
       static const char* GATE_ID   = "gate-a";
       static const char* GATE_NAME = "Gate A";
       ```
    4. Tambahkan struct `GateState` setelah konstanta gate:
       ```cpp
       struct GateState {
         String status      = "idle";   // "granted"|"denied"|"scanning"|"idle"
         String plate       = "";        // nomor plat terbaca
         String anprStatus  = "";        // "detected"|"not_detected"|"low_confidence"
         String gateName    = GATE_NAME; // nama gate dari config
         String greeting    = "Selamat Datang";
       };
       static GateState gGate;
       ```
    5. Tambahkan helper untuk membangun topic string:
       ```cpp
       static String topicEvent()    { return String("parking/") + GATE_ID + "/event"; }
       static String topicGreeting() { return String("parking/") + GATE_ID + "/greeting"; }
       static String topicConfig()   { return String("parking/") + GATE_ID + "/config"; }
       static String topicReset()    { return String("parking/") + GATE_ID + "/reset"; }
       ```
    6. Hapus fungsi `formatSensorValue()` (tidak relevan lagi setelah JSON parser nanti).
       Biarkan `sanitizePayload()` dan `tryParseNumber()` — masih berguna untuk plan berikutnya.
  </action>
  <verify>Build: `pio run` — tidak ada error `kSensors`, `sensorValues`, atau `SensorBinding` undefined.</verify>
  <done>AC-1 satisfied: GateState menggantikan data model lama</done>
</task>

<task type="auto">
  <name>Task 2: Refactor MQTT callback ke 4 handler functions</name>
  <files>src/HD-WF1-WF2-LED-MatrixPanel-DMA.ino.cpp</files>
  <action>
    1. Tambahkan 4 handler function SEBELUM `mqttCallback()`:

       ```cpp
       void handleEventTopic(const String& payload) {
         // JSON parsing akan diimplementasi di plan 01-02
         // Untuk sekarang, simpan raw payload sebagai plate agar display tetap hidup
         gGate.status = "scanning";
         gGate.plate  = payload.substring(0, 12); // placeholder
         redrawNeeded = true;
         Serial.printf("[event] raw: %s\n", payload.c_str());
       }

       void handleGreetingTopic(const String& payload) {
         // JSON parsing di plan 01-02; untuk sekarang simpan raw
         if (payload.length() > 0) {
           gGate.greeting = payload;
         }
         redrawNeeded = true;
         Serial.printf("[greeting] raw: %s\n", payload.c_str());
       }

       void handleConfigTopic(const String& payload) {
         // NVS persistence di plan 01-03; untuk sekarang log saja
         Serial.printf("[config] raw: %s\n", payload.c_str());
       }

       void handleResetTopic(const String& /*payload*/) {
         gGate.status     = "idle";
         gGate.plate      = "";
         gGate.anprStatus = "";
         redrawNeeded     = true;
         Serial.println("[reset] display reset to idle");
       }
       ```

    2. Ganti isi `mqttCallback()` dari loop `kSensors` menjadi routing berdasarkan topic:
       ```cpp
       void mqttCallback(char* topic, byte* payload, unsigned int length) {
         String value;
         value.reserve(length);
         for (unsigned int i = 0; i < length; i++) {
           value += static_cast<char>(payload[i]);
         }
         value.trim();

         const String t(topic);
         if (t == topicEvent())         handleEventTopic(value);
         else if (t == topicGreeting()) handleGreetingTopic(value);
         else if (t == topicConfig())   handleConfigTopic(value);
         else if (t == topicReset())    handleResetTopic(value);
         else Serial.printf("[mqtt] unknown topic: %s\n", topic);
       }
       ```
  </action>
  <verify>Build sukses. Serial monitor setelah publish ke `parking/gate-a/reset` menampilkan "[reset] display reset to idle".</verify>
  <done>AC-3 satisfied: Callback routing ke handler terpisah berfungsi</done>
</task>

<task type="auto">
  <name>Task 3: Ganti subscribe topics dan update renderPanels()</name>
  <files>src/HD-WF1-WF2-LED-MatrixPanel-DMA.ino.cpp</files>
  <action>
    1. Di `connectMqttIfNeeded()`, ganti loop subscribe `kSensors` dengan:
       ```cpp
       mqttClient.subscribe(topicEvent().c_str());
       mqttClient.subscribe(topicGreeting().c_str());
       mqttClient.subscribe(topicConfig().c_str());
       mqttClient.subscribe(topicReset().c_str());
       Serial.printf("Subscribed to parking/%s/* topics\n", GATE_ID);
       ```

    2. Di `renderPanels()`, ganti loop `for (int i = 0; i < 4; i++)` dengan rendering sementara
       berbasis `GateState` (rendering proper akan diimplementasi di Phase 3):
       ```cpp
       void renderPanels() {
         if (!dma_display) return;

         const uint16_t labelColor = dma_display->color565(0, 180, 255);
         const uint16_t valueColor = dma_display->color565(0, 255, 80);
         targetFillScreen(0);

         // P1 (row 0): status
         targetSetTextDefaults();
         targetSetTextColor(labelColor);
         drawCenteredText("STATUS", 0, 1, PANEL_RES_X, 8);
         targetSetTextColor(valueColor);
         drawCenteredText(gGate.status, 0, 12, PANEL_RES_X, 12);

         // P2 (row 1): gate name
         targetSetTextDefaults();
         targetSetTextColor(labelColor);
         drawCenteredText(gGate.gateName, 0, PANEL_RES_Y + 1, PANEL_RES_X, 8);
         targetSetTextColor(valueColor);
         drawCenteredText(gGate.anprStatus.length() ? gGate.anprStatus : "--", 0, PANEL_RES_Y + 12, PANEL_RES_X, 12);

         // P3 (row 2): plate
         targetSetTextDefaults();
         targetSetTextColor(valueColor);
         drawCenteredText(gGate.plate.length() ? gGate.plate : "-- ---- --", 0, PANEL_RES_Y * 2 + 12, PANEL_RES_X, 12);

         // P4 (row 3): greeting
         targetSetTextDefaults();
         targetSetTextColor(labelColor);
         drawCenteredText(gGate.greeting, 0, PANEL_RES_Y * 3 + 12, PANEL_RES_X, 12);
       }
       ```

    Catatan: JANGAN ubah `MqttDisplayVirtualPanel`, `setupDisplayForConfig()`,
    `connectWiFiIfNeeded()`, `setup()`, `loop()`, atau konfigurasi display.
  </action>
  <verify>
    1. `pio run` sukses tanpa error/warning baru.
    2. Serial log setelah connect MQTT menampilkan "Subscribed to parking/gate-a/* topics".
    3. Display tetap render (tidak blank/crash).
  </verify>
  <done>AC-2 dan AC-4 satisfied: Topics baru ter-subscribe, display berfungsi dengan GateState</done>
</task>

</tasks>

<boundaries>

## DO NOT CHANGE
- `MqttDisplayVirtualPanel` class dan `getCoords()` override — koordinat transform sudah dikalibrasi
- `kDisplayConfigs[]` dan `setupDisplayForConfig()` — panel chain config tidak berubah
- `connectWiFiIfNeeded()` — WiFi logic tidak diubah
- `setup()` dan `loop()` — struktur utama tidak berubah di plan ini
- `platformio.ini` — dependency baru (ArduinoJson) ditambah di plan 01-02
- File header `hd-wf1-esp32s2-config.h` dan `hd-wf2-esp32s3-config.h`

## SCOPE LIMITS
- Plan ini TIDAK mengimplementasi JSON parsing (itu plan 01-02)
- Plan ini TIDAK mengimplementasi NVS persistence (itu plan 01-03)
- Plan ini TIDAK mengimplementasi full color rendering P1 (itu Phase 3)
- Plan ini TIDAK mengimplementasi RTC (itu Phase 2)

</boundaries>

<verification>
Sebelum plan dinyatakan selesai:
- [ ] `pio run` kompilasi sukses (0 errors)
- [ ] Tidak ada referensi ke `kSensors`, `sensorValues`, `SensorBinding`, `formatSensorValue` di source
- [ ] `gGate` (GateState) terdefinisi dan digunakan di renderPanels()
- [ ] 4 topic baru ter-subscribe (verifiable via Serial log)
- [ ] 4 handler function ada: `handleEventTopic`, `handleGreetingTopic`, `handleConfigTopic`, `handleResetTopic`
- [ ] `mqttCallback` menggunakan routing berdasarkan topic string comparison
</verification>

<success_criteria>
- Build kompilasi sukses tanpa error baru
- Data model berhasil diganti dari SensorBinding ke GateState
- MQTT subscribe menggunakan topic schema parking gate baru
- Callback routing berfungsi per topic
- Display masih hidup (tidak crash) dengan data default GateState
</success_criteria>

<output>
Setelah selesai, buat `.paul/phases/01-mqtt-json-refactor/01-01-SUMMARY.md`
</output>
