---
phase: 01-mqtt-json-refactor
plan: 03
type: execute
wave: 1
depends_on: ["01-02"]
files_modified:
  - src/HD-WF1-WF2-LED-MatrixPanel-DMA.ino.cpp
autonomous: true
---

<objective>
## Goal
Implementasi `handleConfigTopic()` dengan JSON parser (pola ArduinoJson dari plan 01-02) dan persistence config ke NVS menggunakan Preferences library (built-in ESP32 Arduino). Config dimuat dari NVS saat boot sehingga gate_name dan brightness bertahan setelah restart.

## Purpose
Tanpa NVS, setiap restart firmware kehilangan konfigurasi. Plan ini memastikan `gate_name`, `brightness`, dan `display_timeout` dikonfigurasi sekali via MQTT dan persisten di flash. Ini menutup Phase 1 dan menyiapkan fondasi untuk Phase 4 (idle timer menggunakan `display_timeout`).

## Output
- `src/HD-WF1-WF2-LED-MatrixPanel-DMA.ino.cpp`:
  - `#include <Preferences.h>` ditambahkan
  - Global vars: `gBrightness` (uint8_t) dan `gDisplayTimeout` (uint8_t)
  - Fungsi `loadConfig()`: baca NVS → isi gGate.gateName, gBrightness, gDisplayTimeout
  - Fungsi `saveConfig()`: tulis gGate.gateName, gBrightness, gDisplayTimeout → NVS
  - `setup()`: panggil `loadConfig()` + apply brightness setelah `setupDisplayForConfig()`
  - `handleConfigTopic()`: JSON parse → update fields yang ada → `saveConfig()` → apply brightness
</objective>

<context>
## Project Context
@.paul/PROJECT.md

## Prior Work
@.paul/phases/01-mqtt-json-refactor/01-02-SUMMARY.md

## Source Files
@src/HD-WF1-WF2-LED-MatrixPanel-DMA.ino.cpp
</context>

<acceptance_criteria>

## AC-1: Config dimuat dari NVS saat boot
```gherkin
Given firmware di-restart setelah sebelumnya menerima config MQTT
When setup() dijalankan
Then loadConfig() membaca gate_name, brightness, display_timeout dari NVS namespace "gate-cfg"
  And gGate.gateName terisi dari NVS (bukan hardcoded GATE_NAME)
  And gBrightness diterapkan ke display via setBrightness8()
  And Serial log menampilkan "[config] loaded: gate=... bright=... timeout=..."
```

## AC-2: handleConfigTopic() mem-parse JSON dan update config
```gherkin
Given firmware berjalan
When payload JSON dikirim ke topic "parking/gate-a/config":
  {"gate_name":"Gate A - Lobby","brightness":128,"display_timeout":8}
Then gGate.gateName = "Gate A - Lobby"
  And gBrightness = 128
  And gDisplayTimeout = 8
  And dma_display->setBrightness8(128) dipanggil
  And saveConfig() dipanggil → NVS diupdate
  And redrawNeeded = true
  And Serial log menampilkan nilai yang diupdate
```

## AC-3: handleConfigTopic() update partial (hanya field yang ada)
```gherkin
Given firmware berjalan dengan gate_name="Gate A" brightness=96
When payload JSON hanya berisi {"brightness":64}
Then gBrightness = 64 (diupdate)
  And gGate.gateName = "Gate A" (tidak berubah)
  And gDisplayTimeout tidak berubah
  And saveConfig() tetap dipanggil
```

## AC-4: handleConfigTopic() robust terhadap JSON tidak valid
```gherkin
Given firmware berjalan
When payload bukan JSON valid dikirim ke config topic
Then firmware tidak crash
  And config tidak berubah
  And Serial log menampilkan pesan error parsing
```

## AC-5: Config persist setelah restart
```gherkin
Given config dikirim via MQTT (gate_name="Gate B", brightness=128)
  And saveConfig() berhasil dipanggil
When firmware di-restart (simulasi: loadConfig() dipanggil ulang)
Then gGate.gateName = "Gate B" (dari NVS, bukan default GATE_NAME)
  And gBrightness = 128 (dari NVS, bukan default 96)
```

</acceptance_criteria>

<tasks>

<task type="auto">
  <name>Task 1: NVS infrastructure — Preferences include, globals, loadConfig(), saveConfig()</name>
  <files>src/HD-WF1-WF2-LED-MatrixPanel-DMA.ino.cpp</files>
  <action>
    1. Tambahkan include di bagian atas setelah `#include <ArduinoJson.h>`:
       ```cpp
       #include <Preferences.h>
       ```

    2. Tambahkan global config vars SETELAH deklarasi `static GateState gGate;`
       dan SEBELUM topic helper functions:
       ```cpp
       // ---------------------------------------------------------------------------
       // Persisted config — loaded from NVS on boot, saved when changed via MQTT
       // ---------------------------------------------------------------------------
       static const char* NVS_NAMESPACE    = "gate-cfg";
       static uint8_t     gBrightness      = 96;  // 0-255, default matches setupDisplayForConfig
       static uint8_t     gDisplayTimeout  = 5;   // seconds before returning to idle (Phase 4)
       ```

    3. Tambahkan fungsi `loadConfig()` dan `saveConfig()` SEBELUM fungsi
       `targetFillScreen()` (yaitu sebelum blok "Display helpers"):
       ```cpp
       void loadConfig() {
         Preferences prefs;
         prefs.begin(NVS_NAMESPACE, true); // read-only
         gGate.gateName   = prefs.getString("gate_name", GATE_NAME);
         gBrightness      = prefs.getUChar("brightness",  96);
         gDisplayTimeout  = prefs.getUChar("disp_timeout", 5);
         prefs.end();
         Serial.printf("[config] loaded: gate=%s bright=%u timeout=%u\n",
                       gGate.gateName.c_str(), gBrightness, gDisplayTimeout);
       }

       void saveConfig() {
         Preferences prefs;
         prefs.begin(NVS_NAMESPACE, false); // read-write
         prefs.putString("gate_name",    gGate.gateName);
         prefs.putUChar("brightness",    gBrightness);
         prefs.putUChar("disp_timeout",  gDisplayTimeout);
         prefs.end();
         Serial.println("[config] saved to NVS");
       }
       ```

    4. Di fungsi `setup()`, tambahkan dua baris SETELAH `setupDisplayForConfig(LOCKED_DISPLAY_CONFIG_INDEX)`:
       ```cpp
       loadConfig();
       dma_display->setBrightness8(gBrightness);
       ```
       Letakkan SEBELUM `mqttClient.setBufferSize(512)`.

    Catatan:
    - Preferences adalah library built-in ESP32 Arduino — tidak perlu tambah lib_deps
    - `prefs.begin(namespace, true)` = read-only; `false` = read-write
    - loadConfig() dipanggil setelah setupDisplayForConfig() agar dma_display sudah valid
  </action>
  <verify>
    - Grep "#include <Preferences.h>" di source — ditemukan 1 kali
    - Grep "gBrightness" — ditemukan di deklarasi global + loadConfig + saveConfig + setup
    - Grep "gDisplayTimeout" — ditemukan di deklarasi global + loadConfig + saveConfig
    - Grep "loadConfig()" di setup() — ditemukan setelah setupDisplayForConfig()
  </verify>
  <done>AC-1 dan AC-5 satisfied: NVS infrastructure siap, config dimuat saat boot</done>
</task>

<task type="auto">
  <name>Task 2: Implementasi handleConfigTopic() dengan JSON parser + NVS save</name>
  <files>src/HD-WF1-WF2-LED-MatrixPanel-DMA.ino.cpp</files>
  <action>
    Ganti isi `handleConfigTopic()` yang saat ini hanya berisi Serial.printf dengan implementasi penuh:

    ```cpp
    void handleConfigTopic(const String& payload) {
      JsonDocument doc;
      const DeserializationError err = deserializeJson(doc, payload);

      if (err) {
        Serial.printf("[config] JSON parse error: %s | payload: %s\n",
                      err.c_str(), payload.c_str());
        return;
      }

      bool changed = false;

      // Update hanya field yang ada dalam payload (partial update supported)
      if (doc["gate_name"].is<const char*>()) {
        const String name = doc["gate_name"].as<String>();
        if (name.length() > 0) {
          gGate.gateName = name;
          changed = true;
        }
      }

      if (doc["brightness"].is<int>()) {
        gBrightness = static_cast<uint8_t>(doc["brightness"].as<int>());
        if (dma_display) dma_display->setBrightness8(gBrightness);
        changed = true;
      }

      if (doc["display_timeout"].is<int>()) {
        gDisplayTimeout = static_cast<uint8_t>(doc["display_timeout"].as<int>());
        changed = true;
      }

      if (changed) {
        saveConfig();
        redrawNeeded = true;
        Serial.printf("[config] updated: gate=%s bright=%u timeout=%u\n",
                      gGate.gateName.c_str(), gBrightness, gDisplayTimeout);
      } else {
        Serial.println("[config] no recognized fields in payload — no change");
      }
    }
    ```

    Catatan implementasi:
    - Gunakan `doc["field"].is<Type>()` untuk cek keberadaan field sebelum mengakses —
      ini idiom ArduinoJson v7 yang lebih robust dari `containsKey()` (deprecated)
    - `partial update`: hanya field yang hadir di JSON yang diupdate, yang lain tidak berubah
    - `dma_display` bisa null jika dipanggil sangat awal — cek null sebelum setBrightness8
    - JANGAN ubah handleEventTopic, handleGreetingTopic, atau handleResetTopic
  </action>
  <verify>
    - Grep "handleConfigTopic" — ditemukan definisi fungsi dengan JsonDocument
    - Grep "doc\[.brightness.\].is" — ditemukan di handleConfigTopic
    - Grep "saveConfig()" — ditemukan di handleConfigTopic dan setup
    - Grep "no recognized fields" — ditemukan 1 kali di handleConfigTopic
  </verify>
  <done>AC-2, AC-3, AC-4 satisfied: handleConfigTopic() parse JSON partial, update NVS, apply brightness</done>
</task>

</tasks>

<boundaries>

## DO NOT CHANGE
- `MqttDisplayVirtualPanel` class dan `getCoords()` override
- `kDisplayConfigs[]` dan `setupDisplayForConfig()`
- `connectWiFiIfNeeded()` dan `connectMqttIfNeeded()`
- `handleEventTopic()` dan `handleGreetingTopic()` — sudah selesai di plan 01-02
- `handleResetTopic()` — tidak perlu NVS
- `mqttCallback()` routing logic
- `renderPanels()` — Phase 3
- `GateState` struct definition
- `platformio.ini` — tidak perlu tambah library (Preferences built-in)

## SCOPE LIMITS
- Plan ini TIDAK mengimplementasi idle timer dengan gDisplayTimeout (Phase 4)
- Plan ini TIDAK mengimplementasi OTA update (Phase 4)
- Plan ini TIDAK menyimpan GATE_ID ke NVS — GATE_ID tetap konstanta compile-time
- Display timeout hanya DISIMPAN, belum digunakan dalam logic

</boundaries>

<verification>
Sebelum plan dinyatakan selesai:
- [ ] `#include <Preferences.h>` ada di source
- [ ] `gBrightness` dan `gDisplayTimeout` sebagai global vars
- [ ] `loadConfig()` dan `saveConfig()` terdefinisi
- [ ] `loadConfig()` dipanggil di `setup()` setelah `setupDisplayForConfig()`
- [ ] `dma_display->setBrightness8(gBrightness)` dipanggil di setup() setelah loadConfig()
- [ ] `handleConfigTopic()` menggunakan `JsonDocument` + `doc["field"].is<Type>()`
- [ ] `saveConfig()` dipanggil di `handleConfigTopic()` jika ada perubahan
- [ ] Partial update: hanya field yang ada yang diupdate
</verification>

<success_criteria>
- NVS load/save berfungsi untuk gate_name, brightness, display_timeout
- handleConfigTopic() mendukung partial update (tidak semua field wajib ada)
- Brightness diterapkan langsung ke display saat config diterima
- Config persists setelah restart (dibaca dari NVS saat boot)
- Tidak ada crash jika payload tidak valid
- Phase 1 selesai — MQTT schema, JSON parsing, dan NVS config semua lengkap
</success_criteria>

<output>
Setelah selesai, buat `.paul/phases/01-mqtt-json-refactor/01-03-SUMMARY.md`
</output>
